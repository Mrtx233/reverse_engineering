// 构造方法的定义
// 对象的创建
// symbol处理
var HTMLScriptElement = function HTMLScriptElement() {
    my_env.plugins.throw_error(TypeError, "Illegal constructor");
}
// 处理symbol
my_env.plugins.re_def_sym(HTMLScriptElement);

// toString 搞定了
my_env.plugins.set_native(HTMLScriptElement);


my_env.plugins.def_obj_prop(HTMLScriptElement.prototype, "src", true, true, undefined, undefined,
    function src() {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function src(value) {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLScriptElement.prototype, "type", true, true, undefined, undefined,
    function type() {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function type(value) {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLScriptElement.prototype, "noModule", true, true, undefined, undefined,
    function noModule() {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function noModule(value) {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLScriptElement.prototype, "charset", true, true, undefined, undefined,
    function charset() {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function charset(value) {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLScriptElement.prototype, "async", true, true, undefined, undefined,
    function async() {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function async(value) {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLScriptElement.prototype, "defer", true, true, undefined, undefined,
    function defer() {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function defer(value) {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLScriptElement.prototype, "crossOrigin", true, true, undefined, undefined,
    function crossOrigin() {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function crossOrigin(value) {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLScriptElement.prototype, "text", true, true, undefined, undefined,
    function text() {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function text(value) {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLScriptElement.prototype, "referrerPolicy", true, true, undefined, undefined,
    function referrerPolicy() {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function referrerPolicy(value) {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLScriptElement.prototype, "fetchPriority", true, true, undefined, undefined,
    function fetchPriority() {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function fetchPriority(value) {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLScriptElement.prototype, "event", true, true, undefined, undefined,
    function event() {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function event(value) {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLScriptElement.prototype, "htmlFor", true, true, undefined, undefined,
    function htmlFor() {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function htmlFor(value) {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLScriptElement.prototype, "integrity", true, true, undefined, undefined,
    function integrity() {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function integrity(value) {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLScriptElement.prototype, "blocking", true, true, undefined, undefined,
    function blocking() {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function blocking(value) {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLScriptElement.prototype, "attributionSrc", true, true, undefined, undefined,
    function attributionSrc() {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function attributionSrc(value) {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLScriptElement.prototype, "setAttribute", true, true, true,
    function Fa() {
        if (this instanceof HTMLScriptElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
);
Object.setPrototypeOf(HTMLScriptElement.prototype, HTMLElement.prototype);