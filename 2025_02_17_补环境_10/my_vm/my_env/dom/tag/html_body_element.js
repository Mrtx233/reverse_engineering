// 构造方法的定义
// 对象的创建
// symbol处理
var HTMLBodyElement = function HTMLBodyElement() {
    my_env.plugins.throw_error(TypeError, "Illegal constructor");
}
// 处理symbol
my_env.plugins.re_def_sym(HTMLBodyElement);

// toString 搞定了
my_env.plugins.set_native(HTMLBodyElement);


my_env.plugins.def_obj_prop(HTMLBodyElement.prototype, "text", true, true, undefined, undefined,
    function text() {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function text(value) {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLBodyElement.prototype, "link", true, true, undefined, undefined,
    function link() {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function link(value) {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLBodyElement.prototype, "vLink", true, true, undefined, undefined,
    function vLink() {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function vLink(value) {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLBodyElement.prototype, "aLink", true, true, undefined, undefined,
    function aLink() {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function aLink(value) {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLBodyElement.prototype, "bgColor", true, true, undefined, undefined,
    function bgColor() {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function bgColor(value) {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLBodyElement.prototype, "background", true, true, undefined, undefined,
    function background() {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function background(value) {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLBodyElement.prototype, "onblur", true, true, undefined, undefined,
    function onblur() {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function onblur(value) {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLBodyElement.prototype, "onerror", true, true, undefined, undefined,
    function onerror() {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function onerror(value) {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLBodyElement.prototype, "onfocus", true, true, undefined, undefined,
    function onfocus() {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function onfocus(value) {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLBodyElement.prototype, "onload", true, true, undefined, undefined,
    function onload() {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function onload(value) {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLBodyElement.prototype, "onresize", true, true, undefined, undefined,
    function onresize() {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function onresize(value) {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLBodyElement.prototype, "onscroll", true, true, undefined, undefined,
    function onscroll() {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function onscroll(value) {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLBodyElement.prototype, "onafterprint", true, true, undefined, undefined,
    function onafterprint() {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function onafterprint(value) {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLBodyElement.prototype, "onbeforeprint", true, true, undefined, undefined,
    function onbeforeprint() {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function onbeforeprint(value) {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLBodyElement.prototype, "onbeforeunload", true, true, undefined, undefined,
    function onbeforeunload() {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function onbeforeunload(value) {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLBodyElement.prototype, "onhashchange", true, true, undefined, undefined,
    function onhashchange() {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function onhashchange(value) {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLBodyElement.prototype, "onlanguagechange", true, true, undefined, undefined,
    function onlanguagechange() {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function onlanguagechange(value) {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLBodyElement.prototype, "onmessage", true, true, undefined, undefined,
    function onmessage() {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function onmessage(value) {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLBodyElement.prototype, "onmessageerror", true, true, undefined, undefined,
    function onmessageerror() {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function onmessageerror(value) {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLBodyElement.prototype, "onoffline", true, true, undefined, undefined,
    function onoffline() {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function onoffline(value) {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLBodyElement.prototype, "ononline", true, true, undefined, undefined,
    function ononline() {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function ononline(value) {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLBodyElement.prototype, "onpagehide", true, true, undefined, undefined,
    function onpagehide() {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function onpagehide(value) {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLBodyElement.prototype, "onpageshow", true, true, undefined, undefined,
    function onpageshow() {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function onpageshow(value) {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLBodyElement.prototype, "onpopstate", true, true, undefined, undefined,
    function onpopstate() {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function onpopstate(value) {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLBodyElement.prototype, "onrejectionhandled", true, true, undefined, undefined,
    function onrejectionhandled() {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function onrejectionhandled(value) {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLBodyElement.prototype, "onstorage", true, true, undefined, undefined,
    function onstorage() {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function onstorage(value) {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLBodyElement.prototype, "onunhandledrejection", true, true, undefined, undefined,
    function onunhandledrejection() {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function onunhandledrejection(value) {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLBodyElement.prototype, "onunload", true, true, undefined, undefined,
    function onunload() {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function onunload(value) {
        if (this instanceof HTMLBodyElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);
Object.setPrototypeOf(HTMLBodyElement.prototype, HTMLElement.prototype);