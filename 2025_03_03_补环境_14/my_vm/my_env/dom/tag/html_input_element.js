// 构造方法的定义
// 对象的创建
// symbol处理
var HTMLInputElement = function HTMLInputElement() {
    my_env.plugins.throw_error(TypeError, "Illegal constructor");
}
// 处理symbol
my_env.plugins.re_def_sym(HTMLInputElement);

// toString 搞定了
my_env.plugins.set_native(HTMLInputElement);


my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "accept", true, true, undefined, undefined,
    function accept() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function accept(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "alt", true, true, undefined, undefined,
    function alt() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function alt(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "autocomplete", true, true, undefined, undefined,
    function autocomplete() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function autocomplete(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "defaultChecked", true, true, undefined, undefined,
    function defaultChecked() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function defaultChecked(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "checked", true, true, undefined, undefined,
    function checked() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function checked(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "dirName", true, true, undefined, undefined,
    function dirName() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function dirName(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "disabled", true, true, undefined, undefined,
    function disabled() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function disabled(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "form", true, true, undefined, undefined,
    function form() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    undefined
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "files", true, true, undefined, undefined,
    function files() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function files(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "formAction", true, true, undefined, undefined,
    function formAction() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function formAction(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "formEnctype", true, true, undefined, undefined,
    function formEnctype() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function formEnctype(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "formMethod", true, true, undefined, undefined,
    function formMethod() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function formMethod(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "formNoValidate", true, true, undefined, undefined,
    function formNoValidate() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function formNoValidate(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "formTarget", true, true, undefined, undefined,
    function formTarget() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function formTarget(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "height", true, true, undefined, undefined,
    function height() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function height(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "indeterminate", true, true, undefined, undefined,
    function indeterminate() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function indeterminate(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "list", true, true, undefined, undefined,
    function list() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    undefined
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "max", true, true, undefined, undefined,
    function max() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function max(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "maxLength", true, true, undefined, undefined,
    function maxLength() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function maxLength(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "min", true, true, undefined, undefined,
    function min() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function min(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "minLength", true, true, undefined, undefined,
    function minLength() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function minLength(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "multiple", true, true, undefined, undefined,
    function multiple() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function multiple(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "name", true, true, undefined, undefined,
    function name() {
        if (this instanceof HTMLInputElement) {
            return this.getAttribute("name");
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function name(value) {
        if (this instanceof HTMLInputElement) {
            this.setAttribute("name", value);
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "pattern", true, true, undefined, undefined,
    function pattern() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function pattern(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "placeholder", true, true, undefined, undefined,
    function placeholder() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function placeholder(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "readOnly", true, true, undefined, undefined,
    function readOnly() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function readOnly(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "required", true, true, undefined, undefined,
    function required() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function required(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "size", true, true, undefined, undefined,
    function size() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function size(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "src", true, true, undefined, undefined,
    function src() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function src(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "step", true, true, undefined, undefined,
    function step() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function step(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "type", true, true, undefined, undefined,
    function type() {
        if (this instanceof HTMLInputElement) {
            return this.getAttribute("type");
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function type(value) {
        if (this instanceof HTMLInputElement) {
            this.setAttribute("type", value);
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "defaultValue", true, true, undefined, undefined,
    function defaultValue() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function defaultValue(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "value", true, true, undefined, undefined,
    function value() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function value(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "valueAsDate", true, true, undefined, undefined,
    function valueAsDate() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function valueAsDate(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "valueAsNumber", true, true, undefined, undefined,
    function valueAsNumber() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function valueAsNumber(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "width", true, true, undefined, undefined,
    function width() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function width(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "willValidate", true, true, undefined, undefined,
    function willValidate() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    undefined
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "validity", true, true, undefined, undefined,
    function validity() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    undefined
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "validationMessage", true, true, undefined, undefined,
    function validationMessage() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    undefined
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "labels", true, true, undefined, undefined,
    function labels() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    undefined
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "selectionStart", true, true, undefined, undefined,
    function selectionStart() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function selectionStart(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "selectionEnd", true, true, undefined, undefined,
    function selectionEnd() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function selectionEnd(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "selectionDirection", true, true, undefined, undefined,
    function selectionDirection() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function selectionDirection(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "align", true, true, undefined, undefined,
    function align() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function align(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "useMap", true, true, undefined, undefined,
    function useMap() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function useMap(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "webkitdirectory", true, true, undefined, undefined,
    function webkitdirectory() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function webkitdirectory(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "incremental", true, true, undefined, undefined,
    function incremental() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function incremental(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "popoverTargetElement", true, true, undefined, undefined,
    function popoverTargetElement() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function popoverTargetElement(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "popoverTargetAction", true, true, undefined, undefined,
    function popoverTargetAction() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    function popoverTargetAction(value) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    }
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "checkValidity", true, true, true,
    function checkValidity() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "reportValidity", true, true, true,
    function reportValidity() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "select", true, true, true,
    function select() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "setCustomValidity", true, true, true,
    function setCustomValidity(param0) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "setRangeText", true, true, true,
    function setRangeText(param0) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "setSelectionRange", true, true, true,
    function setSelectionRange(param0, param1) {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "showPicker", true, true, true,
    function showPicker() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "stepDown", true, true, true,
    function stepDown() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "stepUp", true, true, true,
    function stepUp() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
);

my_env.plugins.def_obj_prop(HTMLInputElement.prototype, "webkitEntries", true, true, undefined, undefined,
    function webkitEntries() {
        if (this instanceof HTMLInputElement) {
            debugger;
            return undefined;
        } else {
            my_env.plugins.throw_error(TypeError, "Illegal invocation");
        }
    },
    undefined
);
Object.setPrototypeOf(HTMLInputElement.prototype, HTMLElement.prototype);